name: gitactions-demo-test
on: workflow_dispatch
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: demo message
        run: echo "hi github actions"
  test:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: secret demo
        run: echo ${{secrets.DB_PASSWORD}}
